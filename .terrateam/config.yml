dirs:
  database1:
    when_modified:
      depends_on: 'dir:base'
  database2:
    when_modified:
      depends_on: 'dir:base'
  webservice:
    when_modified:
      depends_on: 'dir:database1 or dir:database2'

workflows:
  - tag_query: 'dir:database1'
    apply:
      - type: run
        cmd: ['exit', '1']
