dirs:
  app:
    when_modified:
      file_patterns: ['${DIR}/*.tf']
  infra:
    when_modified:
      file_patterns: ['${DIR}/*.tf']
engine:
  name: tofu
when_modified:
  prechecks:
    - file_patterns: ['infra/**', '!docs/**']
