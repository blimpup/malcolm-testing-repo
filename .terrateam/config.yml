engine:
  name: terragrunt
  version: 0.69.7
  tf_cmd: tofu

when_modified:
  file_patterns: []

dirs:
  '*/**/terragrunt.hcl':
    when_modified:
      file_patterns: ['${DIR}/*.hcl', '${DIR}/*.tf']

hooks:
  all:
    pre:
      - type: run
        cmd: ['terragrunt', 'terragrunt-info']
      
workflows:
  - tag_query: 'dir:prod/us-east-1/prod/volume'
    engine:
      name: terragrunt
      version: 0.69.3
      tf_cmd: tofu
    plan:
      - type: init
      - type: plan
      - type: run
        cmd: ['bash', '-c', 'terragrunt --version']
      - type: run
        cmd: ['bash', '-c', 'terragrunt --version | grep "0.69.3"']
    apply:
      - type: init
      - type: apply
      - type: run
        cmd: ['bash', '-c', 'terragrunt version | grep "0.69.3"']
  - tag_query: 'dir:prod/us-east-1/prod/compute'
    plan:
      - type: init
      - type: plan
      - type: run
        cmd: ['bash', '-c', 'terragrunt --version']
      - type: run
        cmd: ['bash', '-c', 'terragrunt --version | grep "0.69.7"']
    apply:
      - type: init
      - type: apply
      - type: run
        cmd: ['bash', '-c', 'terragrunt --version | grep "0.69.7"']
