workflows:
  - tag_query: ''
    plan:
      - type: run
        cmd: ['sleep', '120']
      - type: init
      - type: plan

# hooks:
#   all:
#     pre:
#       - type: run
#         cmd: ['sleep', '120']

cost_estimation:
  enabled: false
