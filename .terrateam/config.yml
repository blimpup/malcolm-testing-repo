workflows:
  - tag_query: ''
    plan:
      - type: run
        cmd: ['false']
      - type: init
      - type: plan

notifications:
  summary:
    enabled: true
    mode: pull_request
    plan:
      visible_on: never

engine:
  name: tofu
