engine:
  name: tofu

workflows:
  - tag_query: ''
    plan:
      - type: run
        cmd: ['false']
        ignore_errors: true
        on_error:
          - type: gate
            all_of: ['team:sre']
      - type: init
      - type: plan
          
