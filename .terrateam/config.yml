when_modified:
  autoapply: false

workflows:
  - tag_query: ''
    plan:
      - type: run
        cmd: ['false']
      - type: init
      - type: plan

notifications:
  summary:
    enabled: true
    plan:
      visible_on: failure

engine:
  name: tofu
