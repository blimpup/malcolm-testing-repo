workflows:
  - tag_query: ''
    plan:
      - type: run
        cmd: ['echo', 'TERRATEAM_RUN_KIND=${TERRATEAM_RUN_KIND}=']
        capture_output: true
      - type: run
        cmd: ['exit', '1']
      - type: init
      - type: plan
    apply:
      - type: run
        cmd: ['echo', 'TERRATEAM_RUN_KIND=${TERRATEAM_RUN_KIND}=']
        capture_output: true
      - type: run
        cmd: ['exit', '1']
      - type: init
      - type: apply
