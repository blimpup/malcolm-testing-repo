engine:
  name: tofu


dirs:
  tf:
    workspaces:
      one:
        tags: [one]
      two:
        tags: [two]

workflows:
  - tag_query: ''
    plan:
      - type: init
        extra_args: ['-backend-config=backend_${TERRATEAM_WORKSPACE}.conf']
      - type: plan
