stacks:
  names:
    base:
      tag_query: 'dir:base'
    network:
      tag_query: 'dir:network'
      rules:
        apply_after:
          - base
    service:
      tag_query: 'dir:service'
      rules:
        apply_after:
          - network

engine:
  name: tofu
