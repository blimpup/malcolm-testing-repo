engine:
  name: tofu
workflows:
- environment: development
  tag_query: dir:tf1
  plan:
    - type: run
      cmd: ['sleep', '60']    
    - type: run
      cmd: ['exit', '1']  
- environment: production
  tag_query: dir:tf2
  plan:
    - type: run
      cmd: ['exit', '1']
