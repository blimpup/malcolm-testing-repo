engine:
  name: terraform
  version: 1.9.0
hooks:
  all:
    pre:
    - cmd:
      - test
      - ${TERRATEAM_TF_CMD}
      - '='
      - terraform
      type: run
    - type: run
      cmd: ['${TERRATEAM_TF_CMD}', '--version']
      
