workflows:
  - tag_query: ''
    plan:
      - type: run
        cmd: ['echo', 'TERRATEAM_RUN_KIND=${TERRATEAM_RUN_KIND}=']
        capture_output: true
      # Cheap trick to fail the runs we see the output of the above command
      - type: run
        cmd: ['test', '-n', '']
      - type: init
      - type: plan
    apply:
      - type: run
        cmd: ['echo', 'TERRATEAM_RUN_KIND=${TERRATEAM_RUN_KIND}=']
        capture_output: true
      # Cheap trick to fail the runs we see the output of the above command
      - type: run
        cmd: ['est', '-n', '']
      - type: init
      - type: apply
