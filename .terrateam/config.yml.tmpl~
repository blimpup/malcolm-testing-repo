#! /usr/bin/env bash

set -e
set -u
set -o pipefail

cat << EOF > "$1"
access_control:
  enabled: true
  ci_config_update: ['user:${PLAN_USER}']
  policies:
  - tag_query: ''
    plan: ['*']
    apply: ['*']
EOF
